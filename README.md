# PrepPredict (Offline) — KCET College Predictor

This repository contains an offline version of the PrepPredict project with an Extra Trees model and a Streamlit app.

## Setup

1. Create virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # macOS/Linux
   .\.venv\Scripts\activate    # Windows CMD or PowerShell
